# خطة إطلاق Noor (NUR) على Arc Mainnet — ميزانية 20 دولار

> **الخلاصة أولاً:** عشرون دولاراً **تكفي** لإطلاق عملة حقيقية على Arc Mainnet مع مجمّع سيولة حقيقي على Uniswap.
> عشرون دولاراً **لا تكفي ولا تقترب** من إدراج في بورصة مركزية (CEX)، ولا من تصنيف على CoinMarketCap.
> هذان رقمان مختلفان بعشرة آلاف ضعف، ولا يوجد مسار من الأول إلى الثاني.

---

## 0. القرار المتخذ والحالة الحالية

**القرار:** الخيار (ج) — **نشر على Mainnet فقط بأقل تكلفة**، وتأجيل مجمّع السيولة.

**الحالة: جاهز — في انتظار التمويل.**

| البند | الحالة |
| :--- | :--- |
| محفظة Mainnet | ✅ `0xB66BE4c1C3677a82105da62E447EAde1e243538f` |
| مفتاحها السري | ✅ محفوظ في `SECRETS.md` §3b و `.secrets/mainnet-wallet.json` |
| سكربت النشر | ✅ `ARC_NETWORK=arc-mainnet node scripts/deploy.mjs --yes` |
| سقف الإنفاق التلقائي | ✅ 0.50 دولار (يرفض النشر فوقه) |
| بوابة تأكيد الأموال الحقيقية | ✅ تتطلب `--yes` |
| التحقق على Mainnet | ⚠️ يدوياً من المتصفح (Cloudflare يحجب الأتمتة على `explorer.arc.io`) |
| الرصيد الحالي | **0.00 USDC** |
| محاكاة النشر على Mainnet | ✅ **نجحت — انظر أدناه** |

### محاكاة النشر الفعلية على Mainnet (بلا تكلفة)

نُفّذت على حالة الشبكة الحقيقية بأمرين قراءة-فقط: `eth_call` لتنفيذ الـ constructor فعلاً، و`eth_estimateGas` لقياس الغاز:

| المقياس | النتيجة |
| :--- | :--- |
| `eth_call` (تنفيذ الـ constructor) | ✅ نجح — لا revert |
| كود التشغيل المُرجَع | 7,095 بايت |
| كود التشغيل المتوقع | 7,095 بايت |
| **التطابق** | **تام ✅** — نفس العقد بالضبط |
| الغاز المقيس | 1,831,900 |
| مع هامش 20% | 2,198,280 |
| **أقصى تكلفة** | **0.0879 دولار** |
| سقف الإنفاق | 0.50 دولار — داخل الحد ✅ |

**معنى هذا:** العقد مُثبت أنه يعمل على Mainnet قبل إنفاق أي سنت. النشر ليس تجربة — هو تنفيذ مؤكَّد.

### حمايات الأموال الحقيقية (مُختبرة)

| السكربت | الحماية | النتيجة |
| :--- | :--- | :--- |
| `deploy.mjs` | بوابة `--yes` + سقف 0.50 دولار | ✅ يرفض |
| `distribute.mjs` | بوابة `--yes` | ✅ يرفض |
| `configure.mjs` | بوابة `--yes` | ✅ يرفض |
| `demo.mjs` | بوابة `--yes` | ✅ يرفض |
| `guards.mjs` | بوابة `--yes` | ✅ يرفض |
| `watch-funding.mjs` | مراقبة الرصيد كل 30 ثانية | ✅ يعمل |

### كيف تموّل المحفظة — عبر Base (Binance لا تدعم Arc)

Binance لا تدعم السحب على شبكة Arc. لذلك المسار أصبح: **تُرسل USDC على Base، وأنا أجسرها إلى Arc برمجياً عبر Circle CCTP V2** — دون أي تدخل منك.

**الشيء الوحيد المطلوب منك — عمليتان سحب من Binance إلى نفس العنوان:**

```
①  USDC  على شبكة Base  →  0xB66BE4c1C3677a82105da62E447EAde1e243538f
②  ETH   على شبكة Base  →  0xB66BE4c1C3677a82105da62E447EAde1e243538f
```

**السبب:** غاز Base (للجسر) يُدفع بـ ETH وليس USDC. غاز Base رخيص جداً (0.006 gwei) — أي مبلغ صغير من ETH يكفي لمئات المعاملات. أرسل **أصغر مبلغ تسمح به Binance**.

**المبالغ المقترحة:**
| العملة | الشبكة | المبلغ | الغرض |
| :--- | :--- | :--- | :--- |
| USDC | Base | 3–5 دولار | الجسر + غاز Arc + هامش |
| ETH | Base | أصغر مبلغ مسموح | غاز Base (يكفي جزء من السنت) |

> ⚠️ **الشبكة يجب أن تكون Base تحديداً.** ليست Arc، ولا Ethereum، ولا BNB Chain، ولا Arbitrum.
> إذا اخترت شبكة خاطئة فقد تُفقد الأموال نهائياً.

**ماذا يحدث بعد ذلك — تلقائياً بلا تدخل منك:**
1. أتحقق من وصول USDC و ETH على Base
2. أفوّض TokenMessengerV2
3. أنفّذ `depositForBurnWithHook` مع خدمة التمرير (Forwarding)
4. **Circle تتولى صكّ USDC على Arc نيابة عني** — لذلك لا أحتاج غازاً على Arc مسبقاً
5. أنشر العقد على Arc Mainnet
6. أتحقق من الكود على المستكشف وأحدّث كل السجلات

**التكلفة الفعلية المقيسة:**
| البند | الكلفة |
| :--- | ---: |
| رسوم خدمة التمرير (CCTP) | 0.0159 دولار |
| رسوم البروتوكول | 0% (المستوى القياسي) |
| غاز Base (تفويض + حرق) | أقل من 0.01 دولار |
| غاز Arc (النشر) | 0.088 دولار |
| **الإجمالي** | **~0.11 دولار** |

الأمران الجاهزان:
```bash
node scripts/bridge-to-arc.mjs          # يعرض الأرصدة وتسعيرة الرسوم الحيّة
node scripts/bridge-to-arc.mjs --yes    # ينفّذ الجسر فعلياً
```

### 🧪 البروفة الحقيقية — الجسر مُختبَر فعلياً قبل المال الحقيقي

نفّذت الجسر بالكامل على شبكات الاختبار في الاتجاه المعاكس (Arc Testnet → Base Sepolia)، وهو **نفس الكود تماماً** المستخدم في الإنتاج:

| المرحلة | النتيجة |
| :--- | :--- |
| التفويض (`approve`) | ✅ |
| الحرق (`depositForBurnWithHook`) | ✅ 19.876134 USDC |
| الخطاف (Forwarding hook) | ✅ |
| استطلاع التصديق من Circle | ✅ `pending_confirmations` ← `complete` |
| **صكّ Circle على الوجهة** | ✅ **19.821204 USDC وصلت** |

**معاملة الحرق:** [`0x0f91f088…`](https://testnet.arcscan.app/tx/0x0f91f08898ad4e75ceabfa214c26ca37c6729daed0e4402a72f550f8feea3b27)
**معاملة الصكّ:** [`0x9c3a520e…`](https://sepolia.basescan.org/tx/0x9c3a520e9e16120d6111339116533057ade5de58170f4af3070a080ae690cc32)

#### 🐛 خلل حقيقي اكتشفته البروفة وأصلحته

المحاولة الأولى **فشلت**: `ERC20: transfer amount exceeds balance`.

**السبب:** على Arc، غاز الشبكة هو **نفس** USDC، ويشارك نفس الرصيد مع واجهة ERC-20. معاملة التفويض استهلكت غازاً، فانخفض الرصيد — لكن السكربت كان قد قرأ الرصيد **قبل** التفويض، فحاول حرق أكثر مما تبقّى.

**الإصلاح:** إعادة قراءة الرصيد بعد التفويض، وتقدير غاز معاملة الحرق، وحجز ذلك المبلغ (زائد هامش 0.01 دولار) قبل حساب مبلغ الحرق. ينطبق فقط حين يكون الغاز والعملة المجسورة نفس الأصل.

> هذا الخلل كان سيكلّفنا المحاولة الأولى في الإنتاج. البروفة كشفتها بلا تكلفة حقيقية. الإصلاح مُختبر ويعمل.

#### ✅ تحقق إضافي من واجهة الإنتاج

نفّذت `eth_call` لدالة `depositForBurnWithHook` على عقد **Base Mainnet الحقيقي**:
- المُحدِّد `0x779b432d` فُكّ ترميزه بنجاح
- وصل التنفيذ إلى شرط ERC-20 متوقع: `transfer amount exceeds allowance`

**النتيجة:** الـ ABI والترميز الإنتاجيان صحيحان — الفشل هو المتوقع قبل التفويض، لا خطأ في الترميز.

**سجل البروفة الكامل:** `deployments/bridge-rehearsal.json`
> أرسل مبلغاً تجريبياً صغيراً أولاً إن أردت التحقق من المسار.

**بعد وصول المال، أمران فقط:**
```bash
cd arc-token
ARC_NETWORK=arc-mainnet node scripts/preflight.mjs        # فحص جاهزية شامل (GO / NO-GO)
ARC_NETWORK=arc-mainnet node scripts/deploy.mjs --yes     # النشر
```

**أو أمر واحد ينتظر المال وينشر تلقائياً:**
```bash
node scripts/watch-funding.mjs --wait --deploy
```

### فحص الجاهزية (preflight) — 13 فحصاً قبل إنفاق أي سنت

`scripts/preflight.mjs` يتحقق من كل ما يمكن أن يُهدر المال، **قبل** توقيع أي معاملة:

| # | الفحص | النتيجة الآن على Mainnet |
| ---: | :--- | :--- |
| 1 | هوية الشبكة (chain ID من الـ RPC) | ✅ 5042 |
| 2 | سعر الغاز معقول (< 500 gwei) | ✅ 20 gwei |
| 3 | محفظة العمل موجودة وملف مفتاحها سليم | ✅ |
| 4 | ملف العقد المبني موجود (9,762 بايت) | ✅ |
| 5 | بصمة `standard-json-input` | ✅ `c6c56e74…` |
| 6 | **كود التشغيل يطابق البناء الموثّق على Testnet** | ✅ 7,095 = 7,095 |
| 7 | **مدخلات المترجم لم تتغير منذ البناء الموثّق** | ✅ |
| 8 | **الـ constructor يُنفَّذ فعلاً على حالة Mainnet** | ✅ بلا revert |
| 9 | كود التشغيل المُرجَع يطابق الأصل | ✅ تام |
| 10 | ضمن سقف الإنفاق 0.50 دولار | ✅ 0.088 |
| 11 | الرصيد يغطي أسوأ حالة | ❌ **ناقص 0.088 دولار** |
| 12 | لا يوجد نشر مكرَّر | ✅ |
| 13 | مسار التحقق على المستكشف | ⚠️ يدوي من المتصفح |

**النتيجة: NO-GO — لسبب واحد فقط: الرصيد صفر.** كل فحص تقني آخر ناجح.

---

## 1. الأرقام الفعلية (مقيسة، لا مقدّرة)

قيست مباشرة من `https://rpc.mainnet.arc.io` في 2026-09-17:

| البند | القيمة المقيسة |
| :--- | :--- |
| Chain ID | `5042` |
| سعر الغاز الأساسي | 20 gwei (USDC) |
| حد غاز الكتلة | 30,000,000 |
| **كلفة نشر NoorCoin** | 1,816,318 غاز × 21 gwei = **0.038 دولار** |
| كلفة كل معاملة توزيع | 58,451 غاز ≈ **0.0012 دولار** |
| حالة الكتابة | ✅ **مفتوحة وبلا تفويض** — اختُبرت بتوقيع معاملة فعلية |

### توزيع العشرين دولاراً

| البند | الكلفة | المتبقي |
| :--- | ---: | ---: |
| تحويل USDC إلى Arc (Base → CCTP) | 0.50 – 2.00 | 18.00 – 19.50 |
| نشر العقد على Mainnet | 0.04 | 17.96 – 19.46 |
| التوزيع والتهيئة (5 معاملات) | 0.01 | ~17.95 – 19.45 |
| **المتاح للسيولة** | — | **~18 – 19.5 دولار** |

---

## 2. البنية التحتية المتاحة على Arc Mainnet (متحقَّق منها)

| العقد | الحالة |
| :--- | :--- |
| Uniswap **v2 / v3 / v4 + UniswapX** | ✅ منشورة — Uniswap هو الـ DEX المعتمد لشبكة Arc |
| **Uniswap Pools** (منصة إصدار التوكنات الرسمية) | ✅ بدون رسوم إصدار، الاقتران بـ USDC في v4 |
| Permit2 | ✅ 9,152 بايت |
| Multicall3 / Multicall3From | ✅ منشورة |
| USDC (واجهة ERC-20) | ✅ `0x3600000000000000000000000000000000000000` |

**هذا مهم:** الإطلاق عبر Uniswap Pools لا يتقاضى رسوم إصدار، والسيولة تُقفل بشكل دائم. هذا يجعل الإطلاق بـ 20 دولاراً ممكناً تقنياً.

---

## 3. مراحل التنفيذ

### المرحلة 0 — التمويل (إجراء منك)
تحويل USDC حقيقي إلى محفظة العمل على **Arc Mainnet** تحديداً:
```
address : 0xB66BE4c1C3677a82105da62E447EAde1e243538f
network : Arc Mainnet
chain ID: 5042
```
أرخص مسار مؤكد: بورصة (Kraken / Upbit يدعمان Arc) → سحب مباشر.
المسار البديل: Coinbase → سحب USDC على **Base** → CCTP إلى Arc.

> ⚠️ الشبكة الخاطئة تعني فقدان المال نهائياً. Arc ليست Base ولا Ethereum.

### المرحلة 1 — النشر الرسمي (~0.05 دولار)
1. نشر NoorCoin على Mainnet
2. التحقق من الكود على `explorer.arc.io`
3. بث التوزيع (40/30/20/10) وتفعيل الإعفاءات

### المرحلة 2 — إدراج على DEX (~18 دولاراً)
إنشاء مجمّع **NUR/USDC** على Uniswap v4 عبر Uniswap Pools.
- رسوم الإصدار: **0 دولار**
- السيولة: **تُقفل بشكل دائم** (لا يمكن سحبها لاحقاً)

### المرحلة 3 — الحضور الرقمي (مجاناً)
- صفحة على CoinMarketCap بحالة **Untracked** (بدون سعر أو تصنيف)
- شعار 200×200، whitepaper، موقع، حسابات رسمية
- إدراج العقد في قوائم التوكنات (Uniswap token list)

### المرحلة 4 — النمو (يحتاج رأس مال خارجي)
مؤجلة حتى يتوفر تمويل حقيقي. لا يمكن البدء بها بـ 20 دولاراً.

---

## 4. ما لا يمكن شراؤه بهذا المبلغ — بصراحة

| الهدف | الحد الأدنى الواقعي | 20 دولاراً؟ |
| :--- | :--- | :---: |
| نشر على Mainnet | ~0.05 دولار | ✅ |
| مجمّع سيولة على DEX | ~18 دولاراً | ✅ |
| صفحة CMC (Untracked) | 0 دولار | ✅ |
| **تصنيف CMC متتبَّع** | يتطلب بورصة مؤهَّلة + حجم حقيقي | ❌ |
| **إدراج CEX صغير** | 1,000 – 5,000 دولار | ❌ |
| **إدراج CEX من الطبقة الثانية** | 10,000 – 50,000 دولار | ❌ |
| **إدراج CEX من الطبقة الأولى** | 100,000 – 500,000 دولار + اتفاقيات صناعة سوق | ❌ |
| تدقيق أمني | 5,000 – 30,000 دولار | ❌ |

---

## 5. تقييم صادق للمخاطر

1. **سيولة 18 دولاراً لا تعني شيئاً.** شراء بقيمة 20 دولاراً سيضاعف السعر تقريباً. هذا مجمّع إثبات-مفهوم، وليس سوقاً.
2. **Uniswap Pools تقفل السيولة نهائياً.** الـ 18 دولاراً **تُنفَق ولا تُستثمَر** — لن تستعيدها.
3. **احتمال استرداد العشرين دولاراً منخفض جداً.** اعتبرها تكلفة تعلّم ومحفظة أعمال، لا استثماراً.
4. **لا مسار من 20 دولاراً إلى CEX.** هذا ليس "لاحقاً" — هذا مستحيل بهذا الميزان.
5. **الإطار القانوني:** إصدار عملة يشتريها الناس نشاط منظّم في معظم الدول. لا تُسوّقها كفرصة استثمارية دون مراجعة قانونية.

---

## 6. ماذا يعطيك هذا فعلياً

مقابل 20 دولاراً ستملك:
- عقداً حقيقياً دائماً على شبكة Arc Mainnet، موثّقاً ومقروءاً من أي محفظة
- مجمّع سيولة حقيقياً على Uniswap يمكن لأي شخص التداول فيه فعلاً
- رمزاً قابلاً للإضافة إلى MetaMask وإرساله بين المحافظ
- بنية تقنية وإثبات مفهوم جاهزين للعرض على مستثمر أو شريك

وهذا مكسب حقيقي — لكنه **"مشروع على السلسلة"، وليس "عملة متداولة تجارياً"**.

---

## 7. القرار المطلوب منك

- **أ) امضِ بخطة العشرين دولاراً** — إطلاق Mainnet + مجمّع Uniswap. سأبدأ فور وصول التمويل.
- **ب) أوقف الآن** — واحتفظ بالعشرين دولاراً حتى يتوفر رأس مال يستحق الإطلاق (5,000 دولار فأكثر يفتح مسار CEX صغير).

الخيار (ب) مشروع تماماً. العشرون دولاراً لن تجعلك أقرب إلى CEX بحيث يُذكر.
